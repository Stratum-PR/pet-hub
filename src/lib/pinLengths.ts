/** Employee time-clock PIN length (stored on `employees.pin`). */
export const EMPLOYEE_PIN_LENGTH = 4;

/** Manager kiosk exit PIN length (stored on `businesses.kiosk_manager_pin`). */
export const KIOSK_MANAGER_PIN_LENGTH = 6;
